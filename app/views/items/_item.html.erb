<h1><%= item.title %></h1>
<h2><%= number_to_currency((item.price.to_f/100)) %></h2>
<% if logged_in? %>
  <%= form_for(:cart, url: :carts) do |f| %>
    <%=f.hidden_field :item_id, value: item.id%>
    <%=f.submit "Add to Cart"%>
  <%end%>
<%end%>